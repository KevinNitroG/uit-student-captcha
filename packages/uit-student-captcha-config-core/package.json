{
  "name": "uit-student-captcha-config-core",
  "version": "1.3.2",
  "private": true,
  "type": "module",
  "description": "Shared config schema, defaults, and the postMessage bridge contract used by both the userscript and the config page. Consumed as TypeScript source (no build step).",
  "exports": {
    ".": "./src/index.ts"
  }
}
