{
  // Solution file: holds no sources of its own, just references the sub-projects.
  // `tsc -b` builds them all (app bundle, vite config, vitest specs).
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" },
    { "path": "./tsconfig.spec.json" }
  ]
}
