{
  // Workspace solution file. Holds no sources of its own — it references every
  // package so `tsc -b` from the repo root typechecks the whole monorepo. Each
  // package extends ./tsconfig.base.json for the shared strict compiler options.
  "files": [],
  "references": [
    { "path": "./packages/uit-student-captcha" },
    { "path": "./packages/uit-student-captcha-config-page" }
  ]
}
